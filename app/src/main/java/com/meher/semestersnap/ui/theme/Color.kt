package com.meher.semestersnap.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors
val LightPrimaryBackground = Color(0xFFF8F8F8) // Very light grey/off-white for main background
val LightCardBackground = Color(0xFFFFFFFF)    // Pure white for card backgrounds
val LightTextPrimary = Color(0xFF212121)       // Dark grey for primary text
val LightTextSecondary = Color(0xFF757575)     // Medium grey for secondary text
val LightAccentBlue = Color(0xFF4285F4)        // Blue for accents (e.g., happy face icon)
val LightSuccessGreen = Color(0xFF4CAF50)      // Green for success indicators
val LightProgressBarGreen = Color(0xFF8BC34A)  // Lighter green for progress bars (e.g., battery)
val LightProgressBarBlue = Color(0xFF2196F3)   // Light blue for progress bars (e.g., storage)
val LightProgressBarPurple = Color(0xFF9C27B0) // Purple for progress bars (e.g., memory)
val LightDividerColor = Color(0xFFE0E0E0)      // Light grey for dividers

// Dark Theme Colors
val DarkPrimaryBackground = Color(0xFF000000) // Pure black for main background
val DarkCardBackground = Color(0xFF1C1C1E)    // Dark grey for card backgrounds
val DarkTextPrimary = Color(0xFFFFFFFF)       // White for primary text
val DarkTextSecondary = Color(0xFFAAAAAA)     // Lighter grey for secondary text
val DarkAccentTeal = Color(0xFF26A69A)        // Teal for accents (e.g., happy face icon)
val DarkSuccessGreen = Color(0xFF66BB6A)      // Green for success indicators (e.g., battery full)
val DarkProgressBarBlue = Color(0xFF42A5F5)   // Blue for progress bars (e.g., storage)
val DarkProgressBarPurple = Color(0xFFBA68C8) // Purple for progress bars (e.g., memory)
val DarkDividerColor = Color(0xFF333333)      // Darker grey for dividers
val DarkOptimizeButtonBackground = Color(0xFF3F51B5) // Dark blue for the optimize button background
val DarkOptimizeButtonText = Color(0xFFFFFFFF) // White for the optimize button text